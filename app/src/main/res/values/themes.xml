<resources>
    <!--
        Minimal window-chrome theme for a pure Compose app.
        Theme.DeviceDefault.NoActionBar is guaranteed available on all API 33+ devices.
        Compose handles fullscreen/immersive via WindowInsetsController in MainActivity.
    -->
    <style name="Theme.WebLauncher" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
